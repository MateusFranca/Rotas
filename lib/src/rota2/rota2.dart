import 'package:flutter/material.dart';

class Rota2 extends StatelessWidget {
  const Rota2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rota2'),
      ),
    );
  }
}